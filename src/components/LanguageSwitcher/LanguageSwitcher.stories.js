import React from 'react';
import {storiesOf} from '@storybook/react-native';
import {LanguageSwitcher} from './LanguageSwitcher';

storiesOf('Lanuage switcher', module).add('Language', () => (
  <LanguageSwitcher />
));
